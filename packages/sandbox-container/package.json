{
  "name": "@repo/sandbox-container",
  "version": "0.0.0",
  "private": true,
  "description": "Container runtime for Cloudflare Sandbox SDK",
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && vitest run",
    "clean": "rm -rf dist *.js *.js.map src/**/*.js src/**/*.js.map"
  },
  "dependencies": {
    "@repo/shared-types": "*",
    "zod": "^3.22.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.19",
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  }
}
