---
'@cloudflare/sandbox': patch
---

Apply sandbox environment variables and the `/workspace` default directory to every process launch. Warm pool scale-down now completes container teardown before releasing capacity.
