# Cloudflare Sandbox Bridge

The supported bridge Worker template is on
[`main`](https://github.com/cloudflare/sandbox-sdk/tree/main/bridge/worker).

Use matching stable `@cloudflare/sandbox` and `cloudflare/sandbox` versions.
Documentation: [Sandbox bridge](https://developers.cloudflare.com/sandbox/bridge/).

This directory on `next` is not a supported bridge release target. Deploy from
`main`.
